- name: Printing Variable from Args
  hosts: local
  connection: local
  tasks:
  - name: print the variable by passing the Args
    ansible.builtin.debug:
      msg: "Hi team, I'm {{auth}}, {{course}} is the configuration management tool in {{learn}}"